% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joins.R
\name{awwid_join}
\alias{awwid_join}
\title{Auto-join two AWWID tables using service-defined relationships}
\usage{
awwid_join(x, y, nav, type = "left")
}
\arguments{
\item{x, y}{Data frames to join, or character entity names.}

\item{nav}{Navigation property list from [awwid_metadata_xml()]. Pass a
pre-fetched list to avoid repeated HTTP requests.}

\item{type}{Join type: `"left"` (default), `"inner"`, `"right"`, or
`"full"`.}
}
\value{
A tibble: `x` joined to `y` on the columns defined by the service
  metadata.
}
\description{
Looks up the foreign-key relationship between two tables in a navigation
property list returned by [awwid_metadata_xml()] and performs the
appropriate dplyr join — no need to remember column names.

Both `x` and `y` can be awwid tibbles returned by [awwid_tbl()] (the table
name is inferred automatically from their class) or plain character strings
matching an entity name in `nav` (PascalCase singular, e.g. `"WellReport"`).
}
\examples{
nav  <- awwid_metadata_xml()
con  <- awwid_connect()

wells    <- awwid_tbl(con$wells,    top = 100L)
drillers <- awwid_tbl(con$drillers, top = 100L)

# Table names inferred automatically from the awwid class
awwid_join(wells, drillers, nav)

# Explicit entity names also work
awwid_join("Well", "DrillingCompany", nav)
}
