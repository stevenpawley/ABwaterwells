% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/request.R
\name{awwid_chunk}
\alias{awwid_chunk}
\title{Download a single chunk of an AWWID table}
\usage{
awwid_chunk(table, skip = 0L, top = 10000L, filter = NULL, select = NULL)
}
\arguments{
\item{table}{character string naming the table, or a field from
[awwid_connect()] (e.g. `con$wells`).}

\item{skip}{integer, number of rows to skip (chunk offset).}

\item{top}{integer, number of rows to return.}

\item{filter}{OData filter expression to select rows server-side.}

\item{select}{character vector of columns to return.}
}
\value{
a classed tibble (same class as [awwid_tbl()]).
}
\description{
Downloads one page of rows using explicit `skip` and `top` offsets. Intended
for use with [targets] dynamic branching, where each chunk is an independent
cached target that is only re-downloaded when the upstream count changes.
}
\examples{
con <- awwid_connect()
awwid_chunk(con$wells, skip = 0L,     top = 10000L)
awwid_chunk(con$wells, skip = 10000L, top = 10000L)
}
